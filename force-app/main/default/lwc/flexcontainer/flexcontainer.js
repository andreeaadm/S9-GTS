import { LightningElement, api } from 'lwc';

export default class Flexcontainer extends LightningElement {
    /*** Un-comment the below when LWC supports the ::slotted pseudo and we have a truly generic and reusable flex container for any scenario! (also un-comment CSS) ***/
    /*
    @api colsize;
    get styleClass() {
        return this.colsize ? 'flexcontainer flex-' + this.colsize : 'flexcontainer';
    }
    */
}